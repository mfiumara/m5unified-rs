# Examples

On-device examples will be added once the ESP-IDF component build for the C++ shim is proven.

Planned order:

1. `hello-display` — initialize M5Unified and print text.
2. `button` — change display state on Button A/B.
3. `mic-level` — read microphone samples and print RMS/level over serial.

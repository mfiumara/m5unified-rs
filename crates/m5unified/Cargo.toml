[package]
name = "m5unified"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Safe Rust wrapper for M5Unified."

[lib]
path = "src/lib.rs"

[dependencies]
m5unified-sys.workspace = true
